import 'package:flutter/material.dart';

//se muestra mientras bloc determina si hay un usuario autenticado
class SplashPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Splash Screen'),
      ),
    );
  }
}